---
description: Build and run the uart_checker desktop app
---
Build the Linux release and launch the application:

```
flutter build linux --release
```

Then run from the bundle:

```
./build/linux/x64/release/bundle/uart_checker --working-dir=./build/linux/x64/release/bundle
```
